# Запрещаем прямой доступ к файлам с заявками из браузера.
# CSV с персональными данными не должны быть скачиваемы по прямой ссылке.
Require all denied
# Для старого Apache 2.2:
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
